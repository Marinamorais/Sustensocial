import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    backgroundColor: "#e3f2fd", // Alterei para um tom de azul claro
  },
  title: {
    marginTop: 20,
  },
});

export default styles;
